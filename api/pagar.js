/* ============================================================
   ESENCIA — Función serverless: checkout
   ------------------------------------------------------------
   STUB (Bloque 1): creado como parte de la estructura.
   Se implementa completo en el BLOQUE 3:
   cargo en Culqi v2 + página en Notion (CRM) + email Resend.
   ============================================================ */
export default function handler(req, res) {
  res.status(501).json({ ok: false, error: 'No implementado todavía (Bloque 3)' });
}
