/* ============================================================
   ESENCIA — Función serverless: captura de email (popup)
   ------------------------------------------------------------
   STUB (Bloque 1): creado como parte de la estructura.
   Guardará el email del popup como contacto en el CRM de Notion.
   Se implementa en un bloque posterior.
   ============================================================ */
export default function handler(req, res) {
  res.status(501).json({ ok: false, error: 'No implementado todavía' });
}
